const db = require('../database/models');

const Users = db.User;

const userController = {
    // register: (req, res) => {
    //     res.render('register');
    //   },

}

module.exports = userController;